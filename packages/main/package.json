{
  "name": "@kotoba/main",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@kotoba/shared": "workspace:*"
  },
  "scripts": {
    "dev": "pnpm run typecheck",
    "build": "pnpm run typecheck",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "format": "prettier --check \"src/**/*.{ts,tsx,js,jsx,json,md}\"",
    "test": "vitest --version"
  }
}
